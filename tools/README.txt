"""Optional startup profiler for a Windows development machine.

Run:
    set ASLAN_PROFILE=1
    python main.py

The application writes aslan_startup_profile.txt beside the executable/source.
For actual RAM/CPU measurements on the target PC, use Windows Task Manager
or Resource Monitor while running the same scenario.
"""
